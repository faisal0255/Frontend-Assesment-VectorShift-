# Frontend-Assesment-VectorShift-
A full-stack node-based pipeline builder built with React and FastAPI. Users can create workflows by connecting nodes, and the backend analyzes the pipeline structure by counting nodes, edges, and validating whether it forms a Directed Acyclic Graph (DAG).
